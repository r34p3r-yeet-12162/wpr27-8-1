let myLitObject = {
  name: "John",
  surname: "Doe",
  age: 18,
};
/* let propName = "age"

console.log(myLitObject[propName]);
 */

console.log(myLitObject["name"]);

// We can also use the dot notation
console.log(myLitObject.name);

[1, 2, 3]; // 1
// is there way to only add an element if it does not already exist
